% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_region_plot.R
\name{render_region_plot}
\alias{render_region_plot}
\title{Render a regional plot (helper used by the CM SAF Toolbox)}
\usage{
render_region_plot(
  infile,
  outfile = NULL,
  fileExtension = ".png",
  visualizeVariables,
  visualizeDataMax,
  lon_bounds,
  lat_bounds,
  lon_loc_vec,
  lat_loc_vec,
  name_loc_vec,
  division,
  selectedRegion,
  region_data,
  timestep,
  num_tick,
  num_rmin,
  num_rmax,
  location,
  text1,
  text2,
  text3,
  PAL,
  palettes,
  num_brk,
  reverse,
  textsize,
  bordercolor,
  plot_grid,
  grid_col,
  image_def,
  ihsf,
  nc = NULL
)
}
\arguments{
\item{infile}{Character. Path to the input NetCDF file. Ignored when \code{nc} is provided.}

\item{outfile}{Character or NULL. Output image filename. If NULL, a temporary
file is created with the extension given by \code{fileExtension}.}

\item{fileExtension}{Character. One of ".png", ".jpg", or ".pdf".}

\item{visualizeVariables}{List with plot metadata; must at least contain:
vn (variable name), date.time (available timesteps), and copyrightText.}

\item{visualizeDataMax}{Numeric. Upper bound helper for tick/legend computation.}

\item{lon_bounds}{Numeric length-2. Longitudes used for image dimension calculation.}

\item{lat_bounds}{Numeric length-2. Latitudes used for image dimension calculation.}

\item{lon_loc_vec}{Numeric vector. Longitudes of optional point markers.}

\item{lat_loc_vec}{Numeric vector. Latitudes of optional point markers.}

\item{name_loc_vec}{Character vector. Labels for point markers.}

\item{division}{Character. Column name in region_data used to select a region.
If "COUNTRY", built-in polygons from the rworldxtra package are used.}

\item{selectedRegion}{Character. Region identifier (e.g., ISO3 for "COUNTRY"
or a value found in region_data[\link{division}]).}

\item{region_data}{sf or sp object with polygons. Ignored when division == "COUNTRY".}

\item{timestep}{Scalar selecting the time slice to render, matching
visualizeVariables$date.time.}

\item{num_tick}{Integer. Requested number of legend ticks.}

\item{num_rmin}{Numeric. Lower limit of the color scale (zlim).}

\item{num_rmax}{Numeric. Upper limit of the color scale (zlim).}

\item{location}{Logical. If TRUE, draw point markers and labels from *_loc_vec.}

\item{text1}{Character. Main title.}

\item{text2}{Character. Subtitle/annotation placed via mtext().}

\item{text3}{Character. Legend title (color bar label).}

\item{PAL}{Character. Palette key passed to getColors().}

\item{palettes}{List. Named palettes for getColors().}

\item{num_brk}{Integer. Number of color breaks.}

\item{reverse}{Logical. Reverse palette if TRUE.}

\item{textsize}{Numeric. Base scaling for titles/labels/legend text.}

\item{bordercolor}{Character. Color for region borders and markers.}

\item{plot_grid}{Logical. Upstream flag for grid drawing (not used inside).}

\item{grid_col}{Character. Grid color (not used inside).}

\item{image_def}{List. Image config used by recalculateImageDimensions().}

\item{ihsf}{Numeric or logical. Image height/scale factor for dimension calculation.}

\item{nc}{Optional ncdf4 object. If provided, its filename is used.}
}
\value{
A named list with the following elements:
src (character path to the written image file),
contentType (character MIME type derived from fileExtension),
width (numeric device width; pixels for PNG/JPG, points for PDF\emph{72),
height (numeric device height; pixels for PNG/JPG, points for PDF}72).
}
\description{
Renders a cropped and masked regional map image from a CM SAF NetCDF file and
overlays a selected administrative polygon and optional point locations.
This function is used by the CM SAF R Toolbox UI.
}
\details{
Workflow: read the variable as a raster brick; select the timestep; crop and mask
by the chosen region; compute device dimensions from lon/lat bounds and UI settings;
draw via fields::image.plot(), add borders, optional points/labels, title/subtitle,
and legend. When division == "COUNTRY", the high-resolution countriesHigh dataset
from rworldxtra is used.

CRS & geometry: data and polygons are expected in geographic lon/lat WGS-84 (EPSG:4326).
For sf, ensure st_crs(x) == 4326. Consider sf::sf_use_s2(FALSE) if topology issues arise
with your GEOS/PROJ stack.
}
