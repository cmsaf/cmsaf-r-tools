% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selcountry.R
\name{selcountry}
\alias{selcountry}
\title{Select one or more countries from a CM SAF NetCDF file}
\usage{
selcountry(
  var,
  infile,
  outfile,
  country_code,
  crop = TRUE,
  overwrite = FALSE,
  states = FALSE,
  verbose = FALSE,
  nc = NULL
)
}
\arguments{
\item{var}{Name of NetCDF variable (character).}

\item{infile}{Filename of input NetCDF file. This may include the directory
(character).}

\item{outfile}{Filename of output NetCDF file. This may include the directory
(character).}

\item{country_code}{One or more three-letter ISO3 country codes, all
capitalized or convertible to uppercase (character).}

\item{crop}{Logical; if TRUE, the input file is first cropped to the bounding
box of the selected country or countries before the mask is applied.}

\item{overwrite}{Logical; should an existing output file be overwritten?}

\item{states}{Logical; reserved for future support of sub-national regions.
Currently only country polygons are supported.}

\item{verbose}{Logical; if TRUE, progress messages are shown.}

\item{nc}{Alternatively to \code{infile} you can specify the input as an object of
class \code{ncdf4} as returned from \code{ncdf4::nc_open}.}
}
\value{
A NetCDF file containing the selected country or countries is written.
The return value is the normalized output filename, invisibly.
}
\description{
This function masks a CM SAF NetCDF file by one or more selected countries
and writes the result to a new NetCDF file. The country polygons are taken
from \code{rworldxtra::countriesHigh}. Values outside the selected countries are
set to missing values by adding a one-timestep mask to the input data.
}
\examples{
\dontrun{
selcountry(
  var = "SIS",
  infile = "SIS_input.nc",
  outfile = "SIS_Germany.nc",
  country_code = "DEU",
  crop = TRUE,
  overwrite = TRUE
)

selcountry(
  var = "SIS",
  infile = "SIS_input.nc",
  outfile = "SIS_DACH.nc",
  country_code = c("DEU", "AUT", "CHE"),
  crop = TRUE,
  overwrite = TRUE
)
}
}
\seealso{
Other selection and removal functions: 
\code{\link{extract.level}()},
\code{\link{extract.period}()},
\code{\link{sellonlatbox}()},
\code{\link{selmon}()},
\code{\link{selperiod}()},
\code{\link{selpoint}()},
\code{\link{selpoint.multi}()},
\code{\link{seltime}()},
\code{\link{selyear}()}
}
\concept{selection and removal functions}
